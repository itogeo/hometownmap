# Cloudflare Pages configuration for HometownMap
name = "hometownmap"
compatibility_date = "2024-01-01"

# Pages configuration - output from Next.js static export
pages_build_output_dir = "apps/web/out"

[vars]
NODE_ENV = "production"
